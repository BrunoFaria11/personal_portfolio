import { ApiService } from './api/api';
import { Account } from './schema';
import { Observable } from 'rxjs';
export interface LoginDetails {
    login: string;
    password: string;
}
export declare class AccountService {
    private apiService;
    constructor(apiService: ApiService);
    private getAuthorised(path);
    login(details: LoginDetails): Observable<Account>;
    logout(): Observable<any>;
    spotlight(): Observable<any>;
    lists(): Observable<any>;
    listById(id: number): Observable<any>;
    radios(offset?: number, limit?: number): Observable<any>;
    radio(id: string): Observable<any>;
    private getRequestOptions();
    interact(params: InteractParams): Observable<any>;
}
export interface InteractParams {
    id_track: number;
    id_container: number;
    container_type: ContainerType;
    action: Action;
}
export declare enum ContainerType {
    list = 0,
}
export declare enum Action {
    like = 0,
    dislike = 1,
    skip = 2,
}
