import { Observable, Subject } from 'rxjs/Rx';
export declare class TokenStore {
    private authTokenKey;
    private refreshTokenKey;
    events: Subject<any>;
    get(): Observable<Tokens>;
    save(authToken: any, refreshToken: any): void;
    clear(): void;
}
export interface Tokens {
    authToken: string;
    refreshToken: string;
}
