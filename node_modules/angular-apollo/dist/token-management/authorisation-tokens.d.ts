export declare class AuthorisationTokens {
    authToken: string;
    refreshToken: string;
    hasBeenRefreshed: boolean;
    constructor(authToken: string, refreshToken: string, hasBeenRefreshed?: boolean);
}
