import { Http } from '@angular/http';
import { Observable } from 'rxjs/Rx';
import 'rxjs/add/operator/map';
import { AuthorisationTokens } from './authorisation-tokens';
export declare class RefreshAuthTokenService {
    private http;
    private refreshUrl;
    constructor(http: Http, apiBaseUrl: string);
    refresh(authToken: any, refreshToken: any): Observable<AuthorisationTokens>;
    private getRequestOptions(authToken);
}
