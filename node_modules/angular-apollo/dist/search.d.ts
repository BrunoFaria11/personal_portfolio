import { ApiService } from './api/api';
export declare class SearchService {
    private apiService;
    constructor(apiService: ApiService);
    suggestions(text: string): any;
    search(text: string): any;
}
